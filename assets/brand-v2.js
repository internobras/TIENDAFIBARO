(()=>{'use strict';const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
const meter=$('.scroll-meter span');const onScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;const p=max>0?scrollY/max:0;document.documentElement.style.setProperty('--page-progress',`${p*100}%`);if(meter)meter.style.width=`${p*100}%`};addEventListener('scroll',onScroll,{passive:true});onScroll();
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('is-visible')}),{threshold:.14,rootMargin:'0px 0px -7%'});$$('[data-reveal]').forEach(el=>io.observe(el));
if(!reduced){const stage=$('.alicia-stage');if(stage){const portrait=$('.portrait-wrap',stage);stage.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;portrait.style.transform=`rotateY(${x*9}deg) rotateX(${-y*7}deg) translateZ(14px)`;$$('.service-float',stage).forEach((el,i)=>{const k=(i+1)*4;el.style.setProperty('translate',`${x*k}px ${y*k}px`)})});stage.addEventListener('pointerleave',()=>{portrait.style.transform='';$$('.service-float',stage).forEach(el=>el.style.removeProperty('translate'))})}
$$('.space-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateY(${x*5}deg) rotateX(${-y*4}deg) translateY(-4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}
const story=$('.control-story');if(story){let ticking=false;const updateStory=()=>{const r=story.getBoundingClientRect(),total=story.offsetHeight-innerHeight,p=clamp(-r.top/Math.max(total,1),0,1);story.style.setProperty('--story',p.toFixed(4));const core=$('.control-core',story);if(core&&!reduced)core.style.transform=`translate(-50%,-50%) perspective(900px) rotateX(${8-p*5}deg) rotateY(${-12+p*20}deg) scale(${.88+p*.16})`;const targets=[['.t1',18,28],['.t2',34,18],['.t3',12,75],['.t4',37,81],['.t5',84,25],['.t6',84,76]];targets.forEach(([sel,x,y],i)=>{const el=$(sel,story);if(!el)return;const start=.04+i*.025,local=clamp((p-start)/.58,0,1),cx=61,cy=50,nx=x+(cx-x)*local,ny=y+(cy-y)*local,scale=1-local*.16,rot=(i%2?1:-1)*(1-local)*7;el.style.left=`${nx}%`;el.style.top=`${ny}%`;el.style.bottom='auto';el.style.transform=`translate(-50%,-50%) scale(${scale}) rotate(${rot}deg)`;el.style.opacity=String(1-local*.25)});const copyH=$('.control-copy h2',story),copyP=$('.control-copy p',story),caption=$('.story-caption',story);if(copyH){if(p<.34){copyH.innerHTML='Tú no necesitas<br>comparar 40 tarifas.';copyP.textContent='Facturas, permanencias, precios, compañías, gigas, potencia… el problema no debería convertirse en tu hobby.'}else if(p<.7){copyH.innerHTML='Nos pasas<br>el problema.';copyP.textContent='FÍBARO filtra opciones, condiciones y lo que realmente importa para tu caso.'}else{copyH.innerHTML='Alicia te explica.<br>Tú decides.';copyP.textContent='La tecnología organiza. Una persona real te acompaña. Y ningún cambio se hace sin tu autorización.'}}if(caption)caption.innerHTML=p<.5?'<b>Caos → claridad</b>La información entra. Lo irrelevante sale.': '<b>Control sin complicarte</b>Una sola conversación para entender qué merece la pena.';ticking=false};addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(updateStory)}},{passive:true});updateStory()}
})();