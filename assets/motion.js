(()=>{'use strict';document.documentElement.classList.add('motion-ready');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const q=(s,p=document)=>p.querySelector(s),qa=(s,p=document)=>[...p.querySelectorAll(s)];const meter=q('#scroll-meter');const updateScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;const p=max>0?scrollY/max:0;if(meter)meter.style.width=`${Math.min(1,Math.max(0,p))*100}%`;q('#nav')?.classList.toggle('scrolled',scrollY>16)};addEventListener('scroll',updateScroll,{passive:true});updateScroll();const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){const d=Number(e.target.dataset.delay||0);setTimeout(()=>e.target.classList.add('is-visible'),d);io.unobserve(e.target)}})},{threshold:.12,rootMargin:'0px 0px -5%'});qa('[data-reveal]').forEach(el=>io.observe(el));if(!reduce){qa('[data-tilt]').forEach(el=>{let raf=0;const move=e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{const max=el.classList.contains('control-scene')?5:3.5;el.style.transform=`rotateX(${(-y*max).toFixed(2)}deg) rotateY(${(x*max).toFixed(2)}deg) translateZ(0)`})};const leave=()=>{el.style.transition='transform .7s cubic-bezier(.16,1,.3,1)';el.style.transform='rotateX(0) rotateY(0)';setTimeout(()=>el.style.transition='',700)};el.addEventListener('pointermove',move);el.addEventListener('pointerleave',leave)});qa('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2;el.style.transform=`translate(${x*.08}px,${y*.12}px)`});el.addEventListener('pointerleave',()=>{el.style.transition='transform .5s cubic-bezier(.16,1,.3,1)';el.style.transform='translate(0,0)';setTimeout(()=>el.style.transition='',500)})})}})();